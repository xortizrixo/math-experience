{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "unlocked_all": {
      "type": "boolean",
      "default": false,
      "description": "True after purchasing 'Unlock All Levels'; grants access to every level."
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}